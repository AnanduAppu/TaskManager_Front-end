import React, { useContext } from 'react'

import UseState from './UseState'

function Hooks_display() {

 

//* what is hooks?
    /* hook are functions which allows functional conponent work like as class component, which means before hooks there were life cycle method 
    used to do state management inside react application only we can do in class componnent , it was really to developer then the react hooks were
    introduced , and which make to do state management inside the functional component */




  return (
    <div><UseState/></div>
  )
}

export default Hooks_display